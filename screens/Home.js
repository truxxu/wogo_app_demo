import React from 'react';
import {
  StyleSheet,
  ScrollView,
  View,
  Text,
  Image,
  TextInput,
  TouchableOpacity,
  Alert,
} from 'react-native';

const Home = (props) => {
  return(
    <Text>
      Home
    </Text>
  );
}

const styles = StyleSheet.create({

});

export default Home;
