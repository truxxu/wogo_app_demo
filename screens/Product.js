import React from 'react';
import {
  Text,
  StyleSheet
} from 'react-native';

const Product = (props) => {
  return(
    <Text>
      Product
    </Text>
  );
}

const styles = StyleSheet.create({

});

export default Product;
