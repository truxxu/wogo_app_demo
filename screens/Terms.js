import React from 'react';
import {
  StyleSheet,
  ScrollView,
  View,
  Text,
  Image,
  TextInput,
  TouchableOpacity,
  Alert,
} from 'react-native';

const Terms = ({navigation}) => {
  return(
    <Text>
      Terms
    </Text>
  );
}

const styles = StyleSheet.create({

});

export default Terms;
