import React from 'react';
import {
  Text,
  StyleSheet
} from 'react-native';

const BusinessesMap = (props) => {
  return(
    <Text>
      BusinessesMap
    </Text>
  );
}

const styles = StyleSheet.create({

});

export default BusinessesMap;
